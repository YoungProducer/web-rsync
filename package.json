{
  "name": "web-rsync",
  "version": "1.0.0",
  "main": "index.js",
  "private": false,
  "author": {
    "name": "Alexander Bezrukov"
  },
  "license": "MIT",
  "scripts": {
    "configure": "yarn node-gyp configure",
    "build": "yarn node-gyp configure && yarn build:cpp",
    "build:cpp": "yarn node-gyp build"
  },
  "dependencies": {
    "node-addon-api": "^3.1.0"
  },
  "devDependencies": {
    "node-gyp": "^7.1.2"
  }
}
